import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';

import FlavorForm from './Submit';

ReactDOM.render(
  <React.StrictMode>
    <FlavorForm />
  </React.StrictMode>,
  document.getElementById('root')
);
